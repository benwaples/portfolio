export type FillerType = {
  title: string,
  imageSrc: string,
  caption: string,
  description: string
}

export type ProjectType = {
  title: string,
  imgSrc: string,
  description: string,
  githubLink: string
}

export type OtherProjectType = {
  imgSrc: string,
  githubLink: string,
  deployedLink: string,
}